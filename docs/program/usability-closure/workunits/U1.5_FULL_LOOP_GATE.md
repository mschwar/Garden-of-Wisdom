# U1.5 — Persistent full-loop proof + Gate U1 packet

## Primary lane

Platform/governance + Human curation.

## Why this exists

The programme is successful only when a real passage reaches the actual Garden and survives clean rebuild/reclone.

## Inputs / dependencies

U1.1–U1.4 merged; Gate U0 accepted; one real operator candidate. Prefer the U0 canary if the operator accepted it and still wants it admitted.

## In scope

Drive the real persistent path:
- clean bootstrap from committed mirror;
- locate/use the real candidate or capture a new real one;
- normalize/hints as needed;
- operator accept if not already accepted;
- operator supply minimum admission metadata;
- explicit canonical admission;
- generate unified read model;
- run actual browser;
- search/find/copy the passage;
- inspect visible uncertainty;
- sync committed artifacts;
- fresh-clone/no-SQLite reconstruction;
- regenerate read model;
- repeat browser lookup;
- produce `U1_GATE_PACKET.md`;
- reconcile `docs/PRODUCT_REALITY.md` to the demonstrated full-loop reality without claiming
  Gate U1 acceptance before synthesis.

Update CURRENT to `SYNTHESIS REQUIRED — GATE U1`.

## Out of scope

No W2 research or verification merely to make the gate prettier. If the real passage is unverified, it must remain visibly unverified.

## Human gates

Actual curation/admission decisions and metadata; Gate U1 acceptance.

## Acceptance criteria

One real passage demonstrably survives the complete product loop and clean reconstruction. Every source-of-truth artifact is reconstructible; generated artifacts are reproducible; browser is the real product surface.

## Evidence required

IDs, audit transitions, hashes, projection identity, browser smoke/search evidence, clean-reclone transcript, gate packet.

## Stop condition

Gate U1 packet submitted. STOP. No W2 authorization.

## Builder prompt

Execute U1.5 as the real end-to-end programme gate.

Do not create a synthetic permanent Garden item. Use the U0 canary if it is genuinely wanted/accepted; otherwise ask me for one real passage.

Drive the entire actual persistent loop through the repo's real commands and human gates. Then delete/reconstruct local state from committed artifacts in a fresh-clone-shaped environment, regenerate the read model, and find the same item again in the real browser.

Do not research/verify the quote just to satisfy the test. Preserve uncertainty.

Create `U1_GATE_PACKET.md` and `GARDEN_U1_5_HANDOFF.md`; foreign-QA the gate; resolve; merge; set CURRENT to `SYNTHESIS REQUIRED — GATE U1`; STOP.

## Foreign QA prompt

Reproduce Gate U1 independently from committed artifacts.

Start with no local SQLite and no generated read model if those are rebuildable. Bootstrap, regenerate, and use the browser to find the real admitted item.

Trace its original capture, candidate, curation decision, T-P2 admission, admission metadata, projection row, visible verification state, and browser rendering.

Check all legacy rows still remain coherent.

FAIL if any step requires transcript memory, manual file editing, hidden local state, or research-state inflation.
