# U0.3 — Real persistent operator canary + Gate U0 packet

## Primary lane

Human curation + Platform/governance.

## Why this exists

W1 is heavily tested in throwaway stores. Gate U0 requires ecological proof: a real operator item in the actual persistent store, committed through the mirror, then recovered from scratch.

## Inputs / dependencies

U0.1 and U0.2 merged; real operator passage; existing submit/normalize/hints/review surfaces; current store mirror.

## In scope

- Ask the operator for **one real passage they actually want to put through Garden** and how it was encountered. Do not invent a fake permanent capture.
- Use the real persistent `data/store` path and the documented bootstrap.
- Submit → normalize → hints → show/review.
- Stop for the operator's actual curation decision: accept / hold / reject / duplicate and reason.
- Persist/sync the mirror through the mechanized U0.1 path.
- Prove recovery in a fresh temporary clone/no-SQLite environment from committed artifacts.
- Produce `docs/program/usability-closure/U0_GATE_PACKET.md`.
- Update CURRENT to `SYNTHESIS REQUIRED — GATE U0`.

## Out of scope

Do not canonically admit the item. `eligible` is the furthest allowed corpus state in U0. Do not start U1.

## Human gates

- operator supplies the real passage/capture context;
- operator makes the curation decision;
- operator/frontier accepts Gate U0.

## Acceptance criteria

The real candidate and every relevant audit row survive:
- command completion;
- mirror sync;
- local process restart;
- SQLite deletion;
- bootstrap from committed mirror;
- fresh-clone-shaped reconstruction.

Resulting export is byte-identical before/after recovery.

## Evidence required

Candidate/capture IDs, hashes, decision transcript, mirror status, recovery transcript, gate packet.

## Stop condition

Gate packet submitted and CURRENT says SYNTHESIS REQUIRED. U1.1 remains unauthorized.

## Builder prompt

Execute U0.3 as a real ecological canary, not a fixture.

First verify U0.1/U0.2 are merged and CURRENT authorizes U0.3.

Ask me only for the genuine human-gate input you cannot invent: one real passage I actually want to capture, how I encountered it (capture method/source context), and later my curation decision/reason.

Then drive the actual documented persistent workbench:
bootstrap → submit → normalize → hints → show → operator decision → persisted mirror.

Do not manually edit the export.

Prove recovery by reconstructing from committed artifacts with no local SQLite and compare deterministic export bytes.

Create `U0_GATE_PACKET.md` and `GARDEN_U0_3_HANDOFF.md`. Open PR, get foreign QA, resolve, merge, update CURRENT to `SYNTHESIS REQUIRED — GATE U0`, and STOP. Do not begin U1.1.

## Foreign QA prompt

Independently prove the U0 ecological claim.

From committed PR artifacts, recreate the store in a clean temp/fresh-clone-shaped environment with no SQLite. Locate the real canary candidate, capture, normalization, curation state, and decision history. Compare re-export bytes.

Try to make the mirror stale using a real mutation path; verify the invariant catches/prevents it.

Check the candidate was not canonically admitted and research state was not upgraded.

PASS only if a different machine/context can resume the exact persisted state without transcript knowledge.
