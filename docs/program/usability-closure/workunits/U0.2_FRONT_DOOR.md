# U0.2 — Truthful front door + current-state routing

## Primary lane

Platform/governance.

## Why this exists

The implementation outran the repo's front door: AGENTS/README/program README contain stale W1-era statements. A multi-day agent programme cannot depend on archaeology to answer "where are we?"

## Inputs / dependencies

U0.1 merged; `AGENTS.md`; root `README.md`; `docs/program/README.md`; `docs/queue.md`; `docs/DECISIONS.md`; RUNBOOK; CURRENT; issue #27.

## In scope

- Remove/supersede false current-status claims.
- Make front-door docs point to `docs/program/usability-closure/CURRENT.md` for live programme status rather than duplicating volatile prose.
- Update AGENTS with the actual current command surface and persistence/bootstrap path, compactly.
- Close or correctly disposition issue #27 if satisfied.
- Ensure "how to resume" routes through CURRENT + queue + decisions + unit/handoff.
- Add a lightweight consistency check only if it meaningfully prevents recurrence; do not build a documentation framework.
- Keep historical docs historical; do not rewrite evidence packets to pretend they were written later.

## Out of scope

No persistence mechanics, admission mechanics, browser changes, ontology cleanup, or W2.

## Human gates

None. If two docs claim conflicting authority, preserve the higher-level doctrine and route live status to CURRENT.

## Acceptance criteria

A fresh agent reading only AGENTS + CURRENT + linked canonical docs can answer:
- what Garden is;
- what is usable now;
- W1/Gate B state;
- current gate;
- exactly one READY unit;
- store bootstrap/resume command;
- where to put discovered work;
- what it must not start.

No canonical front-door doc claims that W1 runtime/store does not exist or that W1 is still in progress.

## Evidence required

Before/after contradiction table; grep/search proof for known stale claims; issue #27 disposition; fresh-agent dry-run transcript.

## Stop condition

Front door is truthful. Next unit U0.3.

## Builder prompt

Execute U0.2 exactly as specified.

Treat this as an information-architecture repair, not a prose expansion. Make the smallest set of authoritative front doors truthful and route volatile state through CURRENT.

Specifically audit AGENTS.md, README.md, docs/program/README.md, RUNBOOK, queue and issue #27 against current main. Preserve historical gate reports/handoffs as historical evidence.

Run a "fresh agent" dry run: with no transcript context, can an agent identify the real current surface and exactly one next unit?

Open one PR with a contradiction table in `GARDEN_U0_2_HANDOFF.md`. Stop for QA. Do not start the canary.

## Foreign QA prompt

Review U0.2 as a cold-start agent.

Ignore the builder's explanation at first. Starting from AGENTS.md, follow the documented resume path and see whether you can reconstruct current state without archaeology.

Search for contradictory live-status claims, especially "no W1 runtime/store", "W1 in progress", stale command lists, and ambiguous source-of-truth statements.

Confirm historical packets were not rewritten misleadingly.

PASS only if a new agent is routed to CURRENT/queue/decisions and exactly one READY unit without tacit context.
