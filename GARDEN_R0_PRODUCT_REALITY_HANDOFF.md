# Garden R0 Product Reality & System Reconciliation — Handoff

**Unit:** R0 — Product Reality & System Reconciliation  
**Branch:** `r0/product-reality-reconciliation`  
**Runtime/data scope:** none

## What changed

R0 reconciles the repo around the product it is actually trying to make possible:

- **Passage** is the durable Garden domain object; “quote” is retained where it is a legacy
  CSV/browser term.
- The 324-row religious/philosophical corpus is explicitly the current seed inventory rather than a
  permanent product-scope restriction.
- Garden is the specialized, personally curated passage corpus/product.
- Initiate is the broader lifecycle for arbitrary things worth remembering; no cross-repo
  integration was introduced.
- `docs/PRODUCT_REALITY.md` now carries standing product/system truth.
- `CURRENT.md` remains execution authority and keeps U0.3 as the one READY unit.
- `SYSTEM_MODEL.md` is reconciled to the landed SQLite + deterministic-mirror architecture and to
  canonical ≠ verified.
- The W2–W6 wave map is explicitly planning history; future trajectory is stated as operator
  capabilities and does not authorize those capabilities.
- Further front-door/meta-assurance work is subordinated/deferred unless it becomes a demonstrated
  constraint.

## What deliberately did not change

No Python/runtime, schema, migrations, CSV data, store export, browser code, CI workflow, or
cross-repo integration changed.

## Product validation

The post-R0 primary growth skeleton is:

`encounter → capture → normalize → operator curation → eligible → [missing admission/projection seam] → browser`

The current system walks through `eligible`; U0.3 proves ecological persistence with one real
operator passage; U1 closes the actual Garden-facing seam.

## QA focus

Foreign QA should try to falsify:

1. contradictory product definitions across front doors;
2. any claim that research verification is required for canonical admission;
3. any claim that Initiate now owns Garden or that Garden requires Initiate;
4. any implied runtime/schema/data change;
5. any ambiguity about product reality versus execution authority;
6. any accidental authorization of U1/W2.

## Closeout

After merge, `CURRENT.md` should say:
- last numbered completed U-unit: U0.2;
- last reconciliation: R0;
- single READY unit: U0.3;
- frontier: real persistent operator canary.

This preserves the existing front-door guard's U-unit structural contract without letting that
implementation detail erase the later R0 reconciliation.

R0 is a reconciliation unit, not a new long-running programme.
