# Garden Work Unit — G4: verified Bahá’í homepage preview export

**State:** BLOCKED until Phase 0 is reviewed and the homepage H2B contract is accepted.

## Purpose

Use Garden as a producer of a **small verified Bahá’í collection** that exercises the homepage collection contract.

## Donor set

Human chooses roughly 6–12 Garden records.

Selection guidance:
- Bahá’í only for this experiment;
- preferably non-Hidden-Words passages so switching is obvious;
- short enough for the homepage experience;
- exact quotation/excerpt status must be established;
- avoid known paraphrases unless the contract is intentionally testing them and the homepage explicitly permits them.

## Verification requirements

For each donor:
- authoritative Bahá’í source located;
- wording checked;
- attribution checked;
- source locator checked;
- authoritative URL stored;
- item type recorded;
- verification status promoted only with evidence;
- Garden legacy ID retained as upstream identity.

## Output

Produce a versioned export that conforms exactly to the accepted H2B contract.

Suggested location:

`exports/bahai-homepage-preview/v1/collection.json`

Also emit:
- manifest / provenance report,
- validator output,
- human-readable review table.

No homepage code changes in this repo.
